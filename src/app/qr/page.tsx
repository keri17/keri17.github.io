import QRPageClient from "./QRPageClient";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "ያዲ ክትፎ - QR Code | Scan for Menu",
};

export default function QRPage() {
  return <QRPageClient />;
}
